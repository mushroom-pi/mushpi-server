/** GPIO pin range for the Raspberry Pi Pico RP2040 (29 pins: GP0–GP28). */
export const GPIO_PIN_MIN = 0;
export const GPIO_PIN_MAX = 28;

/** Standard TCP/IP port range. */
export const PORT_MIN = 1;
export const PORT_MAX = 65535;
