export const PAGINATION_DEFAULT_PAGE = 1;
export const PAGINATION_MIN_PAGE = 1;

export const PAGINATION_DEFAULT_LIMIT = 20;
export const PAGINATION_MIN_LIMIT = 1;
export const PAGINATION_MAX_LIMIT = 100;

/** Readings pages can be larger to support time-range data exports. */
export const READINGS_DEFAULT_LIMIT = 100;
export const READINGS_MAX_LIMIT = 500;
